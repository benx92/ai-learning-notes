# 大模型学习文章写作风格规范

> 这份规范用于后续所有大模型学习文章。  
> 目标不是把文章写成权威教材，也不是写成私人日记，而是形成一种稳定的“学习者对外分享”风格：我正在学习，但我会把自己想清楚的部分整理得足够清楚，让读者也能跟着学到东西。

## 一、文章定位

文章默认面向对大模型感兴趣的技术读者，包括程序员、产品技术从业者、AI 应用学习者。

写作身份是：

> 一个正在系统学习大模型的人，把自己的理解过程整理成可公开阅读的技术文章。

不是：

- 不是权威讲师式教程。
- 不是课程内容复述。
- 不是私人备忘录。
- 不是 AI 生成效果展示。
- 不是把所有后续概念提前塞进一篇文章。

## 二、核心语气

整体语气应保持：

- 清楚
- 克制
- 有个人理解
- 有学习过程
- 对读者友好
- 不居高临下

推荐口吻：

```text
更适合把它理解成……
这一点可以先这样看……
这里先不展开细节，只抓住一个主线……
对刚入门的人来说，先分清这两个概念会更稳……
```

避免口吻：

```text
我先记住这个就够了。
我觉得这个对我很有帮助。
我现在不会急着……
我用一个不太严谨但好理解的说法……
```

原因：这些句子太像写给自己的笔记，会让读者不确定文章到底是否面向他。

## 三、第一人称使用规则

可以使用第一人称，但要有边界。

### 可以使用的场景

用于引出学习过程、误解、阶段性变化：

```text
刚开始接触这个概念时，我很容易把它理解成……
继续往下学以后，我发现这个理解还不够准确。
这篇文章想先解决一个基础问题……
```

### 尽量避免的场景

不要在解释技术概念时频繁自我对话：

```text
我先记住……
我可以把它简化理解成……
我现在会这样理解……
这对我提醒很大……
```

应改为更面向读者的表达：

```text
入门阶段可以先记住……
可以先把它简化理解成……
更稳的理解是……
这个提醒很重要……
```

## 四、读者视角规则

文章要让读者感觉：

> 作者是在分享自己的学习路径，但每一段都是为读者整理过的。

所以写作时要少用“我自己内部消化”的句子，多用“读者可以跟着理解”的句子。

| 不推荐 | 推荐 |
| --- | --- |
| 我先假设这些句子已经切好了词 | 为了让例子更直观，下面先使用已经切好词的句子 |
| 我先记住这个简化版就够了 | 入门阶段先抓住这个简化版，就能理解生成过程的主线 |
| 这对我有帮助 | 这个区分能帮助我们避免把模型当成数据库 |
| 我现在更愿意这样理解 | 更稳的理解是 |
| 我目前从四个角度理解它 | 可以从四个角度理解它 |

## 五、文章结构规范

每篇文章建议采用这个节奏：

1. 开头：说明这一篇要解决的主问题。
2. 学习点表格：告诉读者本篇覆盖哪些内容。
3. 常见误解：从一个真实误区切入。
4. 核心解释：用图、表、例子讲清主概念。
5. 实用场景：用工作或开发案例落地。
6. 边界提醒：说明什么时候不能直接相信或直接使用。
7. 总结：收束成 1-3 个核心理解。

不要每篇都写成大而全。  
每篇只解决一个主问题，后续概念可以轻微提到，但不能抢走本篇主线。

## 六、例子选择规范

优先使用真实工作/开发/产品场景：

- 接口变慢排查
- 用户反馈分类
- 会议纪要整理
- 日志分析
- SQL 查询
- 文档问答
- 代码审查
- 需求拆解

谨慎使用“帮我写文章”“帮我写学习笔记”这类例子。  
这类例子太贴近文章生成过程，容易让读者产生“这篇文章是不是直接用 AI 写的”的感觉。

## 七、Markdown 使用规范

每篇文章应合理使用：

- 表格：用于概念对比、风险分类、步骤归纳。
- Mermaid：用于流程、关系、循环、生成过程。
- 代码块：用于 Prompt 示例、伪代码、输入输出。
- 引用块：用于阶段性结论，不要滥用。

Markdown 不只是排版装饰，而是帮助读者降低理解成本。

## 八、原理与数学讲解规范

遇到原理性、算法性、数学性内容时，不能只给结论，也不能只贴公式。需要按“直觉 -> 原理 -> 例子 -> 工程意义”的顺序写清楚。

适用内容包括但不限于：

- Token、上下文窗口、采样参数
- Embedding、向量、维度、语义空间
- 余弦相似度、点积、距离度量
- TF-IDF、BM25、倒排索引
- HNSW、向量数据库、近似最近邻
- MMR、RRF、重排、召回、融合
- RAG 的检索、切分、召回、生成
- Transformer、Attention、概率分布

### 讲解顺序

每个原理尽量按下面顺序展开：

1. 先讲直觉：它想解决什么问题。
2. 再讲机制：它大概怎么工作。
3. 如果有公式，解释每个符号代表什么。
4. 给一个小数字例子，带读者算一遍或走一遍流程。
5. 再给一个工程场景例子，说明它在真实应用里怎么用。
6. 最后说明边界：它什么时候会失效，或者容易被误用。

### 数学公式写法

公式要服务理解，不要为了显得专业而堆公式。

Markdown 中需要使用数学公式能力来表达公式：

- 行内公式用 `$...$`，适合短公式或变量，例如 `$k=5$`、`$P(x)$`。
- 块级公式用 `$$...$$`，适合核心公式、推导步骤、需要单独展示的公式。
- 公式前要先用一句话说明它想解决什么问题。
- 公式后要解释每个符号，不要默认读者知道。
- 重要公式后要给一个小数字例子，把数字代入算一遍。
- 如果公式只是辅助理解，可以用“直觉公式”表达；如果是正式公式，要尽量写准确。

### 公式讲解清单

每次写到重要公式时，必须配套讲解。不要让公式孤零零地出现在文章里。

公式讲解至少包含：

1. **公式解决的问题**：先说明为什么需要这个公式，它用来衡量、计算或优化什么。
2. **公式的直觉含义**：用一句人话解释公式背后的想法。
3. **公式本体**：用 Markdown 数学公式语法写出，核心公式优先用块级公式。
4. **符号解释**：逐个解释公式里的变量、向量、函数、上下标、求和符号。
5. **取值范围**：如果有范围，要说明结果可能落在哪里，例如 $0 \sim 1$、$-1 \sim 1$、非负数等。
6. **计算步骤**：把公式拆成几步，例如先算分子、再算分母、最后相除。
7. **数字代入例子**：至少给一个小数字例子，让读者看到结果怎么算出来。
8. **结果怎么解释**：说明算出来的值代表什么，高低大小意味着什么。
9. **工程场景含义**：说明它在真实项目中怎么用，例如排序、过滤、召回、重排、阈值判断。
10. **常见误用或边界**：说明它什么时候不好用，或者容易被误解在哪里。

可以使用这个固定模板：

```markdown
这个公式想解决的问题是：……

直觉上，它是在衡量……

$$
公式写在这里
$$

其中：

- `$a$` 表示……
- `$b$` 表示……
- `$f(x)$` 表示……

代入一个小例子：

$$
计算过程
$$

这个结果可以这样理解：……

在工程里，它通常用于……

需要注意的是：……
```

推荐写法：

```text
先用一句话解释公式想表达什么。
再用 Markdown 数学公式写出来。
再逐个解释符号。
最后用一个小例子代入数字。
```

例如讲余弦相似度时，不要只写：

$$
\cos(A, B) = \frac{A \cdot B}{\|A\| \|B\|}
$$

要补充：

- `A` 和 `B` 是两段文本对应的向量。
- `A·B` 表示两个向量方向是否接近。
- `|A|` 和 `|B|` 表示向量长度。
- 结果越接近 1，语义方向越接近；越接近 0，相关性越弱。
- 用两组简单二维向量算一次，让读者看到数值怎么来。

例如可以继续写：

$$
A = (1, 0), \quad B = (1, 1)
$$

$$
A \cdot B = 1 \times 1 + 0 \times 1 = 1
$$

$$
\|A\| = 1,\quad \|B\| = \sqrt{1^2 + 1^2} = \sqrt{2}
$$

所以：

$$
\cos(A, B) = \frac{1}{1 \times \sqrt{2}} \approx 0.707
$$

这样读者不仅看到公式，也能看到数字是怎么来的。

### 例子要求

原理文章必须多给好理解的例子，至少包含两类：

- 直觉例子：生活化或工作化，用来降低理解门槛。
- 技术例子：贴近程序员实际工作，比如日志、接口、用户反馈、文档检索、SQL、知识库。

不要只用抽象描述，例如：

```text
向量可以表示语义，相似度可以衡量文本相似。
```

更适合写成：

```text
比如“订单无法支付”和“付款失败”字面不一样，但在客服系统里很可能指向同一类问题。向量检索要解决的，就是让系统不要只按关键词匹配，而是能找到语义上接近的表达。
```

### 扩散性补充规则

在不偏离本篇主线的前提下，可以做适度扩散，帮助读者建立更完整的知识网络。

可以补充：

- 这个概念为什么会出现。
- 它和前后文章的关系。
- 它在真实项目里常见的坑。
- 它和相近概念的区别。
- 它在工程实现里通常如何落地。

但扩散必须服务主问题，不能把文章写成概念堆叠。扩散后要收回来，用一句话说明它和本篇主题的关系。

## 九、后续概念预告规则

可以提到后续概念，但必须控制方式。

推荐：

```text
这里先不展开，后面学到知识库和工具调用时还会继续补上。
```

不推荐：

```text
这部分后面学到 ToolCall、Agent、MCP 时会更重要，因为……
```

原因：过早抛出太多后续术语，会让文章节奏变乱，也会削弱当前主题。

## 十、结尾规范

结尾要回答三个问题：

- 这篇到底想明白了什么？
- 这个理解为什么重要？
- 它和下一步学习有什么关系？

结尾不要写成私人日记式收束：

```text
这就是我这一篇先想明白的事情。
```

更适合写成：

```text
这篇先把一个基础判断立住：模型的回答不是从固定答案库里取出来的，而是在上下文约束下生成出来的。理解这一点，后面再学 Prompt、Token、知识库和工具调用时，很多现象就更容易解释。
```

## 十一、最终风格一句话

后续文章统一按这个标准写：

> 用学习者的真实困惑开头，用技术文章的结构讲清楚；遇到原理和数学要补足直觉、公式、例子和工程意义；最后用克制的个人判断收尾。
